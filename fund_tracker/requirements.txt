pandas
numpy
yfinance
matplotlib
openpyxl
xlsxwriter
streamlit
altair
# tkinter is only needed for main.py's local/desktop file-picker dialog -
# NOT needed to run dashboard.py (incl. hosting it on Streamlit Community
# Cloud), which uses browser uploads instead. It ships with the standard
# Python installer on Windows/macOS; on Linux you may need to install it
# separately, e.g.: sudo apt install python3-tk
# pyxirr  # uncomment once transaction-level cash flow data is available (see Version 2 notes)
